# dependencies / build
node_modules/
dist/
*.tsbuildinfo

# environment & secrets — NEVER commit project data
.env
.env.*
!.env.example

# pod recon OUTPUT (contains internal hostnames, proxy creds, skill IP) — never commit
harness_recon*.txt
harness-recon*.txt
harness_min*.txt
harness-min*.txt
*recon*.txt
*.recon.txt

# harness project data must live OUTSIDE the repo; belt and braces:
harness-data/
profiles/*
!profiles/fixture/
!profiles/example/

# test & tool output
coverage/
playwright-report/
test-results/
*.log

# fixture build output
fixtures/legacy-webapp/target/

# local agent/editor settings
.claude/
.vscode/
.idea/

# OS noise
.DS_Store
Thumbs.db
